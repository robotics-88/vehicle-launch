<launch>

                            <!-- Arguments/configuration -->

    <arg name="simulate" default="false"/>
    <arg name="offline" default="false"/>

                                <!-- ROS nodes -->

    <!-- MAVROS -->
    <let name="fcu_url" value="/dev/cubeorange:57600" unless="$(var simulate)"/>
    <let name="fcu_url" value="udp://127.0.0.1:14551@" if="$(var simulate)"/>
    <include file="$(find-pkg-share mavros)/launch/apm.launch" unless="$(var offline)">
        <arg name="fcu_url" value="$(var fcu_url)"/>
    </include>

    <!-- Simulation -->
    <!-- <include file="$(find-pkg-share airsim_launch)/launch/airsim.xml" if="$(var simulate)"/> -->

    <!-- Feature nodes -->

    <!-- <node pkg="rviz2" exec="rviz2" name="rviz2" args="-d $(find-pkg-share vehicle_launch)/launch/config/distal.rviz" output="log"/> -->

</launch>
